{
  "name": "pradytecai-react",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node --max-old-space-size=4096 ./node_modules/vite/bin/vite.js build",
    "preview": "vite preview",
    "build:check": "vite build && node -e \"const fs=require('fs');const path=require('path');const dist='dist';if(!fs.existsSync(path.join(dist,'index.html'))){console.error('Missing dist/index.html');process.exit(1);}const assets=path.join(dist,'assets');if(!fs.existsSync(assets)||!fs.readdirSync(assets).some(f=>f.endsWith('.js'))){console.error('Missing hashed JS under dist/assets');process.exit(1);}console.log('React build ready for public_html (base /)')\"",
    "test:product-detail": "node scripts/verify-product-detail.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.0"
  }
}
